# project_status.md

项目进度看板。每次做完一轮功能/修复后建议更新本文件（尤其是"已实现功能"和"已知 Bug"两节），保持它和代码实际状态同步。过时信息应直接删掉而不是堆叠，本文件只记录当前真实状态，不是逐次提交的流水账。

## 当前版本

模块化 + Vite 版本，白色工业风 UI。`style.css` + `src/{constants,state,coords,devices,pathfinding,render,history,interactions,main}.js`，`npm install && npm run dev`。仍然无后端、无本地持久化，刷新页面即丢失所有布局。详见 CLAUDE.md 的"技术栈与依赖"一节。

## 已实现功能

**画布基础**
- 无限网格画布（Canvas 2D），`GRID_SIZE=50` px/格，白色/浅灰基调。
- 拖拽空白处平移画布，滚轮缩放（`scale` 限制在 0.2～4 倍）；传送带/管道/箭头/端口/途经点等尺寸类数值统一随缩放等比例变化（`render.js` 的 `scaled()`）。

**设备**
- 从工具栏拖拽生成设备：粉碎机（3×3）、反应池（5×5，四条边分别是传送带入/出、管道入/出）。所有设备统一白底黑边，标签用思源黑体 Black 字重。
- 点击选中（设备内部叠一层浅灰斜纹，选中态下标签文字本身也叠一层灰色斜纹强调），拖拽移动（网格吸附）。
- `R` 键顺时针旋转选中设备 90°（粉碎机/反应池可转；汇流器/分流器朝向由被切入的连线决定，不支持手动旋转）。
- 设备重叠实时检测与半透明红色警示，不阻挡放置。
- `Delete`/`Backspace` 删除选中设备：只删设备本身，相连的传送带/管道保留为悬空的自由网格端点。

**传送带 + 管道双网络**
- 端口按"传送带口/管道口"区分（极简"›"折线箭头，深色=传送带口、蓝色=管道口，未连接半透明/已连接不透明）。
- 汇流器/分流器各有传送带版和管道版（1×1，白底黑边，标签"汇"/"分"），容量上限 3 进 1 出 / 1 进 3 出。
- 正交 A* 寻路，转弯代价 `TURN_PENALTY=3`；同网络内传送带/管道之间禁止物理重叠但允许正交交叉（交叉处渲染"物流桥"）；管道是空中单位，可自由与传送带同格重叠。
- 端口正前方那一格被邻居设备整体占死时，退化为允许贴边拐入（`aStarWithDockingFallback`），入口视觉上仍是笔直对接。
- 支持手动途经点：拖拽新增/移动/双击删除，松手时若导致路径无效自动撤销这次改动。
- 端点重接（拖拽已连接输入口的箭头改接到另一个可用输入口）。
- 传送带带宽约占一格 60%，外侧叠一圈半透明淡灰色描边；管道保持细线条、不叠描边。

**自由传送带/管道模式（`E`/`Q` 键切换）**
- 两次点击流程：起点(输出口/设备本体兜底/空白格) → 实时预览(虚线紧贴端口精确坐标，不留缝隙) → 终点(输入口/已有连线上自动汇流/空白格)落地。
- **悬空端点接续**：起点落在某条连线未接设备的悬空尾部、或终点落在某条连线未接设备的悬空头部时，新画的这一笔会并入那条连线本身（复用同一个 id），而不是新建一条坐标重合的独立连线；单笔同时命中两条不同连线的悬空端点时三段一次融合成一条。不管分几笔画、按什么顺序画，选中/删除/拖拽途经点都能整体生效。
- `Alt+左键` 点击已有连线 → 原地生成分流器节点并自动进入对应画线模式。

**撤销**
- `Ctrl+Z` 撤销，历史栈上限 30 步，覆盖几乎所有会修改画布数据的操作。没有 Redo。

## 后续待办 (TODO)

1. **本地存档 / 读档**：刷新页面即丢失所有数据，最基础的缺口。
2. **Redo（重做）**：已有完整 Undo 历史栈基础设施，加一个重做栈成本不高。
3. **框选 / 批量操作**：目前一次只能选中一个设备或一条连线。
4. **性能优化（增量重算）**：`recomputeAllConnections()`/`recomputeAllPipeConnections()` 每次都是全量重跑，连线数量上去后可能有明显卡顿。
5. **窗口 resize 不重新居中视图**：只重设 Canvas 尺寸并重绘，没有重新调用 `initView()`。

## 已知 Bug / 待优化事项

- 无本地持久化、无 Redo、无框选（同上 TODO）。
- 全量重算无增量优化，连线数量较多时可能有性能瓶颈。
