# project_status.md

项目进度看板。每次做完一轮功能/修复后建议更新本文件（尤其是"已实现功能"和"已知 Bug"两节），保持它和代码实际状态同步。过时信息应直接删掉而不是堆叠，本文件只记录当前真实状态，不是逐次提交的流水账。

## 当前版本

模块化 + Vite 版本，白色工业风 UI。`style.css` + `src/{constants,state,coords,devices,pathfinding,render,history,interactions,main}.js` + `src/data/facilities.js`，`npm install && npm run dev`。仍然无后端、无本地持久化，刷新页面即丢失所有布局。详见 CLAUDE.md 的"技术栈与依赖"一节。

## 已实现功能

**画布基础**
- 无限网格画布（Canvas 2D），`GRID_SIZE=50` px/格，白色/浅灰基调。
- 拖拽空白处平移画布，滚轮缩放（`scale` 限制在 0.2～4 倍）；传送带/管道/箭头/端口/途经点/设备标签等尺寸类数值统一随缩放等比例变化（`render.js` 的 `scaled()`）。

**设备**
- 工具栏左侧栏（150px 宽）：顶部是分类下划线标签页（选中态黑色文字+黑色底部色条，未选中灰色文字，视觉上和设备按钮的白底黑框区分开），下方是当前分类的设备单列长列表，超出高度可滚动。
- 设备数据来自 `src/data/facilities.js` 的 `FACILITIES`（真实游戏基建数据，按【基础生产】【合成制造】【电力】【仓储存取】【其他】分类，同一款建筑的不同模式/配方拆成独立记录，共 45 条），各分类内部顺序对齐官方基建列表顺序。每条记录含 `footprint`/`powerCost`/`powerRange`/`bandwidth`/`isLowProfile`/`ports`（端口 grid 坐标+朝向）等真实数值，落地时经 `devices.js` 的 `SPAWN_TEMPLATES`/`getDevicePorts()` 接入占地/端口/旋转/寻路；`powerCost`/`powerRange` 已接入供电覆盖判定与总功率显示（见下方"电力系统"），`isLowProfile` 已接入管道跨越矮桩体设备的寻路豁免（见下方"传送带 + 管道双网络"），`bandwidth` 目前仍只是数据，未接入任何运行时逻辑。
- 从工具栏拖拽生成设备，落到画布空白处即完成放置；生成时会检查是否顺手挤占了某条操作前合法的连线的必经格子，若破坏则整体撤销这次生成。落地预览会画出端口箭头（方便落地前就看清朝向），且从拖出的那一刻起就可以按 `R` 键预旋转（累计旋转步数存在 `state.spawnRotSteps`，幽灵图标/落地预览尺寸跟着实时更新，落地时固化成新设备的朝向）；开始拖出新设备会立即清空普通模式下残留的单选/框选批量选中，避免拖拽期间按 `R` 转到旧的选中目标上。
- 点击选中（设备内部叠一层浅灰斜纹，选中态下标签文字本身也叠一层灰色斜纹强调），拖拽移动（网格吸附）；单纯点击选中（松手时格子坐标和拖拽前一致）保留选中态，但如果真的把设备拖到了新格子且这次移动被接受，落位后会自动清空选中，不需要再点空白处取消高亮。
- `R` 键顺时针旋转选中设备 90°（落地前也可以旋转，见上一条）；非正方形的真实基建设备旋转会互换 `w`/`h`，和"拖拽设备"一样要检查是否顺手拖坏了别的连线，破坏则整体撤销这次旋转；汇流器/分流器没有 `rot` 字段，靠 `mainOutEdge`/`mainInEdge` 直接顺时针 +1 实现同样的旋转效果，落地前后都支持。
- 设备重叠实时检测与半透明红色警示，不阻挡放置。
- `Delete`/`Backspace` 删除选中设备：只删设备本身，相连的传送带/管道保留为悬空的自由网格端点。
- 设备标签文字过长时按"括号前优先断行"规则自动换行；缩放到极小、连缩略后的两行都放不下时，退化为"首字+省略号"。

**传送带 + 管道双网络**
- 端口按"传送带口/管道口"区分（极简"›"折线箭头，深色=传送带口、蓝色=管道口，未连接半透明/已连接不透明），箭头朝向统一朝内（对接方向）。普通模式下悬停在已连接端口（输入口或输出口均可）上、或正在拖拽一段连线划过候选目标端口时，端口会有三态视觉反馈（`hover`=实心细描边+亮色填充，光标变普通指针；`target`=只变填充色、描边不变，纯粹的落点预览），见 `render.js` 的 `drawPortMarker`。
- 汇流器/分流器各有传送带版和管道版（1×1，白底黑边，标签"汇"/"分"），容量上限 3 进 1 出 / 1 进 3 出。
- 正交 A* 寻路，转弯代价 `TURN_PENALTY=3`；同网络内传送带/管道之间禁止物理重叠但允许正交交叉（交叉处渲染"物流桥"）；管道是空中单位，可自由与传送带同格重叠，也可以直接从矮桩体设备（`isLowProfile`，目前仅仓库存货口/取货口）正上方跨过而不用绕路；传送带仍然贴地铺设，遇到矮桩体设备一样要绕开。管道系统（寻路 + 管道版汇流器/分流器）完全豁免地图边界，传送带（寻路 + 传送带版汇流器/分流器）仍受边界约束（见下方"地图系统"）。
- 终点"笔直接入"不是唯一允许的到达方式：端口正前方那一格被邻居设备整体占死、或"贴边拐入"比绕远路更省格数时，A* 在同一次搜索里就会选中贴边拐入，不是两阶段严格寻路失败后才退化的兜底，入口视觉上仍是笔直对接（详见 CLAUDE.md「A* 寻路代价值规则」）。
- 支持手动途经点：拖拽新增/移动/双击删除，松手时若导致路径无效自动撤销这次改动。
- 端点重接：拖拽已连接的输入口或输出口箭头都能改接到另一个可用端口（四种方向对称支持：输入/输出 × 传送带/管道），不再只支持输入口一侧。
- 拖拽设备移位后，会清空该设备相连连线的手动途经点并重新按最优路径寻路，而不是保留旧途经点硬凑。
- 传送带带宽约占一格 60%，外侧叠一圈半透明淡灰色描边；管道保持细线条、不叠描边。
- 不成立（`invalid`，找不到合法路径）的连线用红色警示渲染；选中态会换成更饱和/更不透明的红色（`INVALID_COLOR`/`INVALID_COLOR_SELECTED`）区别于未选中状态，之前两者视觉完全一样，点了看不出选没选中。

**地图系统**
- 4 张可选地图（四号谷地主基地 70×70、四号谷地副基地 40×40、武陵主基地 80×80、武陵副基地 50×50，`src/data/maps.js` 的 `MAP_CATALOG`），右上角地图选择器切换；切换会清空当前布局，切换前弹出自定义二次确认浮层（`#map-confirm-overlay`，不用原生 `window.confirm()`——沙盒 iframe 环境会静默拦截原生 confirm 导致地图切换功能整个失效，这是修过的真实 bug）。
- 每张地图固定内置一个不可删除、不可复制的核心设备（主基地=协议核心，副基地=次级核心），但可以正常移动/旋转/连线；核心已从工具栏拖拽生成列表里去掉，不能重复放置。
- 非对称边界规则：只有核心、传送带版汇流器/分流器、仓库存取线源桩与基段这几类"强边界"设备（`devices.js` 的 `requiresMapBounds()`）受地图矩形限制，其余设备（含全部电力设备）不受限。
- 越界/弄坏连线不会撤回当前操作：框选拖拽、框选旋转、`Ctrl+V` 粘贴、单设备拖拽、单设备旋转（`R`）、工具栏落地新设备这 6 类"放置类"操作永远成功、留在原地，越界设备复用"设备重叠"同一套半透明红色警示+红描边（`devices.js` 的 `computeOutOfBoundsIds()`）。只有"生成新连线/新节点"的 2 处操作（自由传送带/管道画新线落地、`Alt+左键` 生成汇流器/分流器）仍保留"弄坏一条操作前合法的连线就整体撤销"的安全网。

**自由传送带/管道模式（`E`/`Q` 键切换）**
- 两次点击流程：起点(输出口/设备本体兜底/空白格) → 实时预览(虚线紧贴端口精确坐标，不留缝隙) → 终点(输入口/已有连线上自动汇流/空白格)落地。
- **悬空端点接续**：起点落在某条连线未接设备的悬空尾部、或终点落在某条连线未接设备的悬空头部时，新画的这一笔会并入那条连线本身（复用同一个 id），而不是新建一条坐标重合的独立连线；单笔同时命中两条不同连线的悬空端点时三段一次融合成一条。不管分几笔画、按什么顺序画，选中/删除/拖拽途经点都能整体生效。
- `Alt+左键` 点击已有连线 → 原地生成分流器节点并自动进入对应画线模式。

**撤销**
- `Ctrl+Z` 撤销，历史栈上限 30 步，覆盖几乎所有会修改画布数据的操作。没有 Redo。

**电力系统**
- 供电桩/中继器（含息壤版）以自身占地中心为中心产生方形无线供电范围（供电桩 12×12、中继器 7×7，`facilities.js` 的 `powerRange`）；耗电设备（`powerCost < 0`）只要占地和某个供电范围有部分重叠就算通电，不单独维护 `needsPower` 布尔字段。
- 通电判定是纯派生计算（`devices.js` 的 `computeUnpoweredIds`/`getPowerRangeRect`/`computePowerRangeRects`，每次从 `state.devices` 现算，不进撤销栈），不需要用电或已通电的设备无视觉提示；断电的设备在右上角显示一个红色圆形感叹号徽标（复用"警告统一红色"语义，`WARNING_COLOR`），鼠标悬停会弹出"设备未通电"文字提示浮窗。
- `H` 键切换全局供电覆盖范围叠层（半透明黄橙色 + 虚线描边，`POWER_RANGE_FILL`/`POWER_RANGE_STROKE`），一次性显示所有供电桩/中继器的范围，多个范围重叠处颜色自然叠加变深；从工具栏拖拽生成供电桩/中继器时，落地预览阶段也会顺带显示即将生效的范围，不用等落地后再按 H 键。
- 屏幕右上角常驻显示当前所有已放置设备 `powerCost` 之和（"总功率: ±N"），实时跟随设备增删/拖拽/撤销更新。
- 纯视觉/信息层面的功能，不影响寻路、连线或任何产能模拟——断电设备不会真的停止"生产"，因为吞吐/产能模拟本身还没做（见下方 TODO）。

**惰气/酸气环境覆盖判定**
- 气体散布机（`dev_气体散布机`）接入惰气/酸气后，以自身占地中心为中心产生 13×13 的环境覆盖范围（`devices.js` 的 `getGasEnvRangeRect`），类型跟着接入的气体走；接入速率同其它槽位判定一样不做数值核实，只看有没有接对气体种类。
- 需要惰气/酸气环境的配方（`recipes.js` 的 `environmentCondition` 文案里带"惰气环境"/"酸气环境"字样）一旦被当前槽位选择命中、且设备占地没有被同类型范围完全覆盖，复用既有警告图标+悬停浮窗框架提示"需求惰气/酸气环境"，和"未通电"等其它警告互不干扰、可同时出现。
- `H` 键切换全局叠层（半透明蓝/橙色块 + 虚线描边，惰气蓝/酸气橙区分），和供电范围共用同一个开关；拖拽已选气体的散布机时不按 H 也有实时预览。**下一步待改**：见下方 TODO，要去掉填充色、改成常驻边框（不再挂在 H 键下）。

**框选批量操作**
- 不是需要 `X` 键切换进入/退出的独立模式（早期版本是，长按已选中项才能触发批量拖动，实测手感别扭，已改掉），普通模式下随时可用：`Ctrl+拖拽` 拉矩形框选，单击点选可以增减单个设备/连线，与普通单选共用同一套黄色选中视觉；与自由传送带/管道模式互斥（进入这两个画线工具会清空当前框选批量选中）。
- 按住已选中项直接拖拽即整体移动（刚体平移，选区内所有设备/相关连线共用同一个整体格偏移，不需要长按等待）；`R` 键整体绕选区中心批量旋转（拖动进行中禁止旋转，避免拖动快照过期导致"整体转向"退化成"各转各的"）；`Delete` 批量删除；`Ctrl+C`/`Ctrl+V` 复制粘贴（粘贴预览跟随鼠标悬停格，保持设备间相对位置不变）。
- 只有一端在被删/被移集合内的连线会被正确处理（分离为悬空自由端点或跟着一起移动），不会牵连到和这次操作无关的其它连线。
- 点击框选集合之外的任何东西、`右键`、`Esc` 都会清空当前框选批量选中。

## 后续待办 (TODO)

1. **本地存档 / 读档**：刷新页面即丢失所有数据，最基础的缺口。
2. **Redo（重做）**：已有完整 Undo 历史栈基础设施，加一个重做栈成本不高。
3. **性能优化（增量重算）**：`recomputeAllConnections()`/`recomputeAllPipeConnections()` 每次都是全量重跑，连线数量上去后可能有明显卡顿。
4. **窗口 resize 不重新居中视图**：只重设 Canvas 尺寸并重绘，没有重新调用 `initView()`。
5. **吞吐/产能计算逻辑**：`facilities.js` 的 `bandwidth` 目前只是静态数据，没有接入任何产能计算玩法逻辑（供电覆盖判定已经接入，见上方"电力系统"）。
6. **惰气/酸气环境范围叠层改为常驻边框**：现在的画法（半透明色块填充 + 虚线描边，挂在 `H` 键供电范围开关下）要改成——不要填充色，只保留边框；且不再需要按 `H` 才能看到，气体散布机一旦接好气体就常驻显示范围边框（不受 `H` 键状态影响，供电范围叠层本身不变，仍然按 `H` 切换）。涉及 `render.js` 的 `drawGasEnvRanges`/`drawGasEnvRangeRect`。
7. **反应池/扩容反应池的槽位面板**：`facilities.js` 里 `slots.sharedPool: true` 的这两条记录目前右键只显示"暂未支持"提示（`interactions.js` 的 `renderRecipePanel`），配方数据本身已经在 `recipes.js` 里（`dev_反应池`/`dev_扩容反应池`）。需要设计"共享槽位"UI 和匹配逻辑——不是固定的 `inputSolid`/`inputFluid`/`outputSolid`/`outputFluid` 四槽位模型，而是所有原料产物共用同一个槽位池（`slots.totalSlots` 个），具体怎么在面板上区分"这个槽位现在被当成原料还是产物"、配方匹配规则怎么套用，还没和用户定下来，需要先讨论方案再实现。

## 已知 Bug / 待优化事项

- 无本地持久化、无 Redo（同上 TODO）。
- 全量重算无增量优化，连线数量较多时可能有性能瓶颈。
