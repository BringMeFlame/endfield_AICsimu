{
  "name": "endfield-aicsimu",
  "private": true,
  "version": "0.1.0",
  "description": "《明日方舟：终末地》风格的极简基建布局模拟器",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "fetch:icons": "node scripts/fetch-item-icons.mjs",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.54.3",
    "vite": "^8.2.2",
    "wrangler": "^4.128.0"
  }
}
